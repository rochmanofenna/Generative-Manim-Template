# Core API + rendering. Requires Python 3.11 (manim 0.18.0 pulls Pillow 9.5.0,
# which has no wheels for 3.12+ and does not compile against current libwebp).
#
# System packages are NOT installed by pip -- you also need:
#   ffmpeg, a LaTeX distribution (latex/pdflatex), dvisvgm, cairo, pango
flask==2.2.2
gunicorn==20.1.0
werkzeug==2.3.7
flask-cors==3.0.10
python-dotenv==1.0.0
openai>=1.24.0,<2.0.0
# 0.37.1 predates the current Claude models; >=0.122.0 is what this was tested on.
anthropic>=0.122.0,<1.0.0
requests>=2.31.0
manim==0.18.0
# manim 0.18.0 imports pkg_resources at startup, which setuptools 81 removed.
setuptools<81

# Optional cloud storage backends. Only needed when USE_LOCAL_STORAGE=false;
# both are imported lazily so local rendering works without them.
# google-cloud-storage==2.10.0
# azure-storage-blob==12.18.2
